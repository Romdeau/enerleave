<div class="page-header">
  <h1>Leave Tracking</h1>
</div>
<div class="paragraph">
  <h2><%= "#{@user.email} has #{@user.leave_request.count} leave requests"%></h2>

  <%= render 'leave_requests/leave' %>

  <%= link_to "Back", user_path(@user) %>
</div>
